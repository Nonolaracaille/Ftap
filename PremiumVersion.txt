-- Configuration
local KEY = "44 72 65 61 6d 41 72 65 4e 69 67 68 74 6d 61 72 65 73" -- Changez cette clé

-- Services
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local mouse = player:GetMouse()

-- Création du ScreenGui principal
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "VenomMenu"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.DisplayOrder = 999999
screenGui.IgnoreGuiInset = true
screenGui.Parent = playerGui

-- Frame de connexion (système de clé)
local loginFrame = Instance.new("Frame")
loginFrame.Name = "LoginFrame"
loginFrame.Size = UDim2.new(0, 400, 0, 250)
loginFrame.Position = UDim2.new(0.5, -200, 0.5, -125)
loginFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
loginFrame.BorderSizePixel = 0
loginFrame.Parent = screenGui

local loginCorner = Instance.new("UICorner")
loginCorner.CornerRadius = UDim.new(0, 12)
loginCorner.Parent = loginFrame

-- Bordure rainbow pour login
local loginBorder = Instance.new("Frame")
loginBorder.Name = "Border"
loginBorder.Size = UDim2.new(1, 6, 1, 6)
loginBorder.Position = UDim2.new(0, -3, 0, -3)
loginBorder.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
loginBorder.BorderSizePixel = 0
loginBorder.ZIndex = loginFrame.ZIndex - 1
loginBorder.Parent = loginFrame

local borderCorner = Instance.new("UICorner")
borderCorner.CornerRadius = UDim.new(0, 12)
borderCorner.Parent = loginBorder

-- Titre login
local loginTitle = Instance.new("TextLabel")
loginTitle.Size = UDim2.new(1, 0, 0, 50)
loginTitle.BackgroundTransparency = 1
loginTitle.Text = "🔐 VENOM AUTH"
loginTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
loginTitle.TextSize = 24
loginTitle.Font = Enum.Font.GothamBold
loginTitle.Parent = loginFrame

-- TextBox pour la clé
local keyBox = Instance.new("TextBox")
keyBox.Size = UDim2.new(0.8, 0, 0, 40)
keyBox.Position = UDim2.new(0.1, 0, 0.35, 0)
keyBox.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
keyBox.BorderSizePixel = 0
keyBox.PlaceholderText = "Entrez la clé..."
keyBox.Text = ""
keyBox.TextColor3 = Color3.fromRGB(255, 255, 255)
keyBox.TextSize = 16
keyBox.Font = Enum.Font.Gotham
keyBox.ClearTextOnFocus = false
keyBox.Parent = loginFrame

local keyBoxCorner = Instance.new("UICorner")
keyBoxCorner.CornerRadius = UDim.new(0, 8)
keyBoxCorner.Parent = keyBox

-- Bouton de validation
local submitBtn = Instance.new("TextButton")
submitBtn.Size = UDim2.new(0.8, 0, 0, 40)
submitBtn.Position = UDim2.new(0.1, 0, 0.6, 0)
submitBtn.BackgroundColor3 = Color3.fromRGB(75, 0, 130)
submitBtn.BorderSizePixel = 0
submitBtn.Text = "VALIDER"
submitBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
submitBtn.TextSize = 18
submitBtn.Font = Enum.Font.GothamBold
submitBtn.Parent = loginFrame

local submitCorner = Instance.new("UICorner")
submitCorner.CornerRadius = UDim.new(0, 8)
submitCorner.Parent = submitBtn

-- Message de statut
local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(1, 0, 0, 30)
statusLabel.Position = UDim2.new(0, 0, 0.85, 0)
statusLabel.BackgroundTransparency = 1
statusLabel.Text = ""
statusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
statusLabel.TextSize = 14
statusLabel.Font = Enum.Font.Gotham
statusLabel.Parent = loginFrame

-- Menu principal (caché au départ)
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 550, 0, 400)
mainFrame.Position = UDim2.new(0.5, -275, 0.5, -200)
mainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
mainFrame.BorderSizePixel = 0
mainFrame.Visible = false
mainFrame.Parent = screenGui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 12)
mainCorner.Parent = mainFrame

-- Bordure rainbow pour menu principal
local mainBorder = Instance.new("Frame")
mainBorder.Name = "Border"
mainBorder.Size = UDim2.new(1, 6, 1, 6)
mainBorder.Position = UDim2.new(0, -3, 0, -3)
mainBorder.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
mainBorder.BorderSizePixel = 0
mainBorder.ZIndex = mainFrame.ZIndex - 1
mainBorder.Parent = mainFrame

local mainBorderCorner = Instance.new("UICorner")
mainBorderCorner.CornerRadius = UDim.new(0, 12)
mainBorderCorner.Parent = mainBorder

-- Barre de titre
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 50)
titleBar.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
titleBar.BorderSizePixel = 0
titleBar.Parent = mainFrame

local titleCorner = Instance.new("UICorner")
titleCorner.CornerRadius = UDim.new(0, 12)
titleCorner.Parent = titleBar

-- Titre
local title = Instance.new("TextLabel")
title.Size = UDim2.new(0.8, 0, 1, 0)
title.BackgroundTransparency = 1
title.Text = "⚡ VENOM MENU [PREMIUM VERSION]"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextSize = 20
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Left
title.Position = UDim2.new(0, 15, 0, 0)
title.Parent = titleBar

-- Bouton fermer
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 40, 0, 40)
closeBtn.Position = UDim2.new(1, -45, 0, 5)
closeBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
closeBtn.BorderSizePixel = 0
closeBtn.Text = "✕"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.TextSize = 20
closeBtn.Font = Enum.Font.GothamBold
closeBtn.Parent = titleBar

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 8)
closeCorner.Parent = closeBtn

-- Panel des sections
local sectionPanel = Instance.new("Frame")
sectionPanel.Size = UDim2.new(0, 150, 1, -60)
sectionPanel.Position = UDim2.new(0, 10, 0, 60)
sectionPanel.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
sectionPanel.BorderSizePixel = 0
sectionPanel.Parent = mainFrame

local panelCorner = Instance.new("UICorner")
panelCorner.CornerRadius = UDim.new(0, 8)
panelCorner.Parent = sectionPanel

local sectionList = Instance.new("UIListLayout")
sectionList.Padding = UDim.new(0, 8)
sectionList.Parent = sectionPanel

-- Contenu
local contentFrame = Instance.new("Frame")
contentFrame.Size = UDim2.new(0, 370, 1, -60)
contentFrame.Position = UDim2.new(0, 170, 0, 60)
contentFrame.BackgroundTransparency = 1
contentFrame.Parent = mainFrame

-- Fonction pour créer un bouton de section
local function createSectionButton(text, index)
    local btn = Instance.new("TextButton")
    btn.Name = text
    btn.Size = UDim2.new(1, -10, 0, 35)
    btn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    btn.BorderSizePixel = 0
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
    btn.TextSize = 14
    btn.Font = Enum.Font.Gotham
    btn.Parent = sectionPanel
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 6)
    btnCorner.Parent = btn
    
    return btn
end

-- Créer les sections
local mainBtn = createSectionButton("🏠 Principal", 1)
local espBtn = createSectionButton("👁️ ESP", 2)
local playerBtn = createSectionButton("👤 Joueur", 3)
local scriptsBtn = createSectionButton("📜 Scripts", 4)

-- Section principale avec le bouton Fling
local mainSection = Instance.new("ScrollingFrame")
mainSection.Size = UDim2.new(1, 0, 1, 0)
mainSection.BackgroundTransparency = 1
mainSection.ScrollBarThickness = 4
mainSection.Parent = contentFrame

local mainList = Instance.new("UIListLayout")
mainList.Padding = UDim.new(0, 10)
mainList.Parent = mainSection

-- Bouton Fling Script
local flingBtn = Instance.new("TextButton")
flingBtn.Size = UDim2.new(1, -20, 0, 50)
flingBtn.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
flingBtn.BorderSizePixel = 0
flingBtn.Text = "Premium Version V2 made by DreamLover"
flingBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
flingBtn.TextSize = 16
flingBtn.Font = Enum.Font.GothamBold
flingBtn.Parent = mainSection

local flingCorner = Instance.new("UICorner")
flingCorner.CornerRadius = UDim.new(0, 8)
flingCorner.Parent = flingBtn

-- Sections ESP, Joueur (vides pour l'instant)
local espSection = Instance.new("ScrollingFrame")
espSection.Size = UDim2.new(1, 0, 1, 0)
espSection.BackgroundTransparency = 1
espSection.ScrollBarThickness = 4
espSection.Visible = false
espSection.CanvasSize = UDim2.new(0, 0, 0, 200)
espSection.Parent = contentFrame

local espList = Instance.new("UIListLayout")
espList.Padding = UDim.new(0, 10)
espList.Parent = espSection

-- Variables ESP
local espEnabled = false
local usernameEspEnabled = false
local espHighlights = {}
local espLabels = {}

-- Fonction pour créer un toggle
local function createToggle(name, parent, callback)
    local toggleFrame = Instance.new("Frame")
    toggleFrame.Size = UDim2.new(1, -20, 0, 50)
    toggleFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    toggleFrame.BorderSizePixel = 0
    toggleFrame.Parent = parent
    
    local toggleCorner = Instance.new("UICorner")
    toggleCorner.CornerRadius = UDim.new(0, 8)
    toggleCorner.Parent = toggleFrame
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.7, 0, 1, 0)
    label.Position = UDim2.new(0, 15, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = name
    label.TextColor3 = Color3.fromRGB(255, 255, 255)
    label.TextSize = 14
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = toggleFrame
    
    local toggleBtn = Instance.new("TextButton")
    toggleBtn.Size = UDim2.new(0, 80, 0, 30)
    toggleBtn.Position = UDim2.new(1, -90, 0.5, -15)
    toggleBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
    toggleBtn.BorderSizePixel = 0
    toggleBtn.Text = "OFF"
    toggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    toggleBtn.TextSize = 14
    toggleBtn.Font = Enum.Font.GothamBold
    toggleBtn.Parent = toggleFrame
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 6)
    btnCorner.Parent = toggleBtn
    
    local isOn = false
    
    toggleBtn.MouseButton1Click:Connect(function()
        isOn = not isOn
        if isOn then
            toggleBtn.Text = "ON"
            toggleBtn.BackgroundColor3 = Color3.fromRGB(50, 200, 50)
        else
            toggleBtn.Text = "OFF"
            toggleBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        end
        callback(isOn)
    end)
    
    return toggleFrame
end

-- Fonction pour activer ESP rouge
local function toggleESP(enabled)
    espEnabled = enabled
    
    if enabled then
        for _, plr in pairs(Players:GetPlayers()) do
            if plr ~= player and plr.Character then
                local char = plr.Character
                local highlight = Instance.new("Highlight")
                highlight.Name = "ESPHighlight"
                highlight.FillColor = Color3.fromRGB(255, 0, 0)
                highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
                highlight.FillTransparency = 0.5
                highlight.OutlineTransparency = 0
                highlight.Parent = char
                espHighlights[plr] = highlight
            end
        end
        
        -- Gérer les nouveaux joueurs
        Players.PlayerAdded:Connect(function(plr)
            if espEnabled then
                plr.CharacterAdded:Connect(function(char)
                    wait(0.5)
                    if espEnabled then
                        local highlight = Instance.new("Highlight")
                        highlight.Name = "ESPHighlight"
                        highlight.FillColor = Color3.fromRGB(255, 0, 0)
                        highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
                        highlight.FillTransparency = 0.5
                        highlight.OutlineTransparency = 0
                        highlight.Parent = char
                        espHighlights[plr] = highlight
                    end
                end)
            end
        end)
    else
        for plr, highlight in pairs(espHighlights) do
            if highlight then
                highlight:Destroy()
            end
        end
        espHighlights = {}
    end
end

-- Fonction pour activer Username ESP
local function toggleUsernameESP(enabled)
    usernameEspEnabled = enabled
    
    if enabled then
        for _, plr in pairs(Players:GetPlayers()) do
            if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                local hrp = plr.Character.HumanoidRootPart
                
                local billboardGui = Instance.new("BillboardGui")
                billboardGui.Name = "UsernameESP"
                billboardGui.Adornee = hrp
                billboardGui.Size = UDim2.new(0, 200, 0, 50)
                billboardGui.StudsOffset = Vector3.new(0, 3, 0)
                billboardGui.AlwaysOnTop = true
                billboardGui.Parent = hrp
                
                local nameLabel = Instance.new("TextLabel")
                nameLabel.Size = UDim2.new(1, 0, 1, 0)
                nameLabel.BackgroundTransparency = 1
                nameLabel.Text = plr.Name
                nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
                nameLabel.TextSize = 16
                nameLabel.Font = Enum.Font.GothamBold
                nameLabel.TextStrokeTransparency = 0.5
                nameLabel.Parent = billboardGui
                
                local distanceLabel = Instance.new("TextLabel")
                distanceLabel.Size = UDim2.new(1, 0, 0.5, 0)
                distanceLabel.Position = UDim2.new(0, 0, 0.5, 0)
                distanceLabel.BackgroundTransparency = 1
                distanceLabel.Text = "0 studs"
                distanceLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
                distanceLabel.TextSize = 14
                distanceLabel.Font = Enum.Font.Gotham
                distanceLabel.TextStrokeTransparency = 0.5
                distanceLabel.Parent = billboardGui
                
                espLabels[plr] = {billboardGui, distanceLabel}
            end
        end
        
        -- Mise à jour des distances
        RunService.RenderStepped:Connect(function()
            if usernameEspEnabled and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                local myHRP = player.Character.HumanoidRootPart
                for plr, data in pairs(espLabels) do
                    if plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                        local distance = math.floor((myHRP.Position - plr.Character.HumanoidRootPart.Position).Magnitude)
                        data[2].Text = distance .. " studs"
                    end
                end
            end
        end)
        
        -- Gérer les nouveaux joueurs
        Players.PlayerAdded:Connect(function(plr)
            if usernameEspEnabled then
                plr.CharacterAdded:Connect(function(char)
                    wait(0.5)
                    if usernameEspEnabled and char:FindFirstChild("HumanoidRootPart") then
                        local hrp = char.HumanoidRootPart
                        
                        local billboardGui = Instance.new("BillboardGui")
                        billboardGui.Name = "UsernameESP"
                        billboardGui.Adornee = hrp
                        billboardGui.Size = UDim2.new(0, 200, 0, 50)
                        billboardGui.StudsOffset = Vector3.new(0, 3, 0)
                        billboardGui.AlwaysOnTop = true
                        billboardGui.Parent = hrp
                        
                        local nameLabel = Instance.new("TextLabel")
                        nameLabel.Size = UDim2.new(1, 0, 1, 0)
                        nameLabel.BackgroundTransparency = 1
                        nameLabel.Text = plr.Name
                        nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
                        nameLabel.TextSize = 16
                        nameLabel.Font = Enum.Font.GothamBold
                        nameLabel.TextStrokeTransparency = 0.5
                        nameLabel.Parent = billboardGui
                        
                        local distanceLabel = Instance.new("TextLabel")
                        distanceLabel.Size = UDim2.new(1, 0, 0.5, 0)
                        distanceLabel.Position = UDim2.new(0, 0, 0.5, 0)
                        distanceLabel.BackgroundTransparency = 1
                        distanceLabel.Text = "0 studs"
                        distanceLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
                        distanceLabel.TextSize = 14
                        distanceLabel.Font = Enum.Font.Gotham
                        distanceLabel.TextStrokeTransparency = 0.5
                        distanceLabel.Parent = billboardGui
                        
                        espLabels[plr] = {billboardGui, distanceLabel}
                    end
                end)
            end
        end)
    else
        for plr, data in pairs(espLabels) do
            if data[1] then
                data[1]:Destroy()
            end
        end
        espLabels = {}
    end
end

-- Créer les toggles ESP
createToggle("🔴 ESP Rouge", espSection, toggleESP)
createToggle("👤 Username ESP", espSection, toggleUsernameESP)

local playerSection = Instance.new("ScrollingFrame")
playerSection.Size = UDim2.new(1, 0, 1, 0)
playerSection.BackgroundTransparency = 1
playerSection.ScrollBarThickness = 4
playerSection.Visible = false
playerSection.Parent = contentFrame

-- Layout joueur
local playerList = Instance.new("UIListLayout")
playerList.Padding = UDim.new(0, 10)
playerList.Parent = playerSection

-- Fonction pour récupérer le Humanoid
local function getHumanoid()
    local char = player.Character or player.CharacterAdded:Wait()
    return char:WaitForChild("Humanoid")
end

-- Fonction création slider simple
local function createValueChanger(title, defaultValue, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -20, 0, 60)
    frame.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    frame.BorderSizePixel = 0
    frame.Parent = playerSection

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = frame

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.6, 0, 1, 0)
    label.Position = UDim2.new(0, 10, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = title
    label.TextColor3 = Color3.fromRGB(255, 255, 255)
    label.TextSize = 14
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = frame

    local box = Instance.new("TextBox")
    box.Size = UDim2.new(0, 80, 0, 30)
    box.Position = UDim2.new(1, -170, 0.5, -15)
    box.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    box.BorderSizePixel = 0
    box.Text = tostring(defaultValue)
    box.TextColor3 = Color3.fromRGB(255, 255, 255)
    box.TextSize = 14
    box.Font = Enum.Font.Gotham
    box.Parent = frame

    local boxCorner = Instance.new("UICorner")
    boxCorner.CornerRadius = UDim.new(0, 6)
    boxCorner.Parent = box

    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 70, 0, 30)
    btn.Position = UDim2.new(1, -85, 0.5, -15)
    btn.BackgroundColor3 = Color3.fromRGB(75, 0, 130)
    btn.BorderSizePixel = 0
    btn.Text = "APPLIQUER"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 12
    btn.Font = Enum.Font.GothamBold
    btn.Parent = frame

    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 6)
    btnCorner.Parent = btn

    btn.MouseButton1Click:Connect(function()
        local value = tonumber(box.Text)
        if value then
            callback(value)
        end
    end)
end

-- WalkSpeed
createValueChanger("🏃 WalkSpeed", 16, function(value)
    forcedWalkSpeed = math.clamp(value, 1, 300)
end)

-- JumpPower
createValueChanger("🦘 JumpPower", 50, function(value)
    getHumanoid().JumpPower = math.clamp(value, 1, 300)
end)

-- Section Scripts avec tous les scripts
local scriptsSection = Instance.new("ScrollingFrame")
scriptsSection.Size = UDim2.new(1, 0, 1, 0)
scriptsSection.BackgroundTransparency = 1
scriptsSection.ScrollBarThickness = 4
scriptsSection.Visible = false
scriptsSection.CanvasSize = UDim2.new(0, 0, 0, 700)
scriptsSection.Parent = contentFrame

local scriptsList = Instance.new("UIListLayout")
scriptsList.Padding = UDim.new(0, 10)
scriptsList.Parent = scriptsSection

-- Fonction pour créer un bouton de script
local function createScriptButton(name, url)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -20, 0, 50)
    btn.BackgroundColor3 = Color3.fromRGB(60, 60, 80)
    btn.BorderSizePixel = 0
    btn.RichText = true
    btn.Text = "▶️ " .. name
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 14
    btn.Font = Enum.Font.GothamBold
    btn.Parent = scriptsSection
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = btn
    
    btn.MouseButton1Click:Connect(function()
        btn.Text = "⏳ CHARGEMENT..."
        btn.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
        wait(0.5)
        
        local success, err = pcall(function()
            loadstring(game:HttpGet(url))()
        end)
        
        if success then
            btn.Text = "✅ " .. name
            btn.BackgroundColor3 = Color3.fromRGB(50, 200, 50)
        else
            btn.Text = "❌ ERREUR"
            btn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
            warn("Erreur:", err)
        end
        
        wait(2)
        btn.Text = "▶️ " .. name
        btn.BackgroundColor3 = Color3.fromRGB(60, 60, 80)
    end)
    
    return btn
end

-- Créer tous les boutons de scripts
createScriptButton('<font color="rgb(255,215,0)">PREMIUM 👑 </font>- Tokra Script', "https://raw.githubusercontent.com/sladostrastnik/TokraScript/refs/heads/main/Loader.luau")

createScriptButton('<font color="rgb(255,215,0)">PREMIUM 👑 </font>- Friendly', "https://raw.githubusercontent.com/Brovaky/Friendly/refs/heads/main/Friendly")

createScriptButton('<font color="rgb(255,215,0)">PREMIUM 👑 </font>- Bloody Script', "https://raw.githubusercontent.com/BloodyV2/BloodyScript/refs/heads/main/Free")

createScriptButton('<font color="rgb(255,215,0)">PREMIUM 👑 </font>- SRV9 Loader', "https://srv9.xyz/scripts/loader")

createScriptButton("- Cosmic Hub", "https://raw.githubusercontent.com/Jeffepicy/CosmicHub/refs/heads/main/CosmicHub")
createScriptButton("- FTAP", "https://raw.githubusercontent.com/BlizTBr/scripts/main/FTAP.lua")
createScriptButton("- The Chosen One Lite", "https://raw.githubusercontent.com/blueEa1532/thechosenone/refs/heads/main/The_Chosen_One_Lite")
createScriptButton("- RTX Universal", "https://rawscripts.net/raw/Universal-Script-RTX-working-on-delta-not-in-krnl-42100")
createScriptButton("- PShade Ultimate", "https://raw.githubusercontent.com/randomstring0/pshade-ultimate/refs/heads/main/src/cd.lua")
createScriptButton("- Custom Script", "https://pastebin.com/raw/fN9beEL6")

-- Animation rainbow
local function animateRainbow(frame)
    spawn(function()
        local hue = 0
        while frame.Parent do
            hue = (hue + 0.5) % 360
            frame.BackgroundColor3 = Color3.fromHSV(hue / 360, 1, 1)
            wait(0.03)
        end
    end)
end

animateRainbow(loginBorder)
animateRainbow(mainBorder)

-- Rendre le menu draggable
local dragging, dragInput, dragStart, startPos

local function update(input)
    local delta = input.Position - dragStart
    mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

titleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

titleBar.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        update(input)
    end
end)

-- Validation de la clé
submitBtn.MouseButton1Click:Connect(function()
    if keyBox.Text == KEY then
        statusLabel.Text = "✅ Accès autorisé!"
        statusLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
        wait(1)
        loginFrame.Visible = false
        mainFrame.Visible = true
    else
        statusLabel.Text = "❌ Clé incorrecte!"
        statusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
        keyBox.Text = ""
    end
end)

-- Bouton fermer
closeBtn.MouseButton1Click:Connect(function()
    mainFrame.Visible = false
end)

-- Toggle avec la touche T (ouvrir et fermer)
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    
    if input.KeyCode == Enum.KeyCode.T then
        if not loginFrame.Visible then
            -- Toggle le menu principal (ouvrir/fermer)
            mainFrame.Visible = not mainFrame.Visible
        end
    end
end)



-- Gestion des sections
mainBtn.MouseButton1Click:Connect(function()
    mainSection.Visible = true
    espSection.Visible = false
    playerSection.Visible = false
    scriptsSection.Visible = false
    mainBtn.BackgroundColor3 = Color3.fromRGB(75, 0, 130)
    espBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    playerBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    scriptsBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
end)

espBtn.MouseButton1Click:Connect(function()
    mainSection.Visible = false
    espSection.Visible = true
    playerSection.Visible = false
    scriptsSection.Visible = false
    mainBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    espBtn.BackgroundColor3 = Color3.fromRGB(75, 0, 130)
    playerBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    scriptsBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
end)

playerBtn.MouseButton1Click:Connect(function()
    mainSection.Visible = false
    espSection.Visible = false
    playerSection.Visible = true
    scriptsSection.Visible = false
    mainBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    espBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    playerBtn.BackgroundColor3 = Color3.fromRGB(75, 0, 130)
    scriptsBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
end)

scriptsBtn.MouseButton1Click:Connect(function()
    mainSection.Visible = false
    espSection.Visible = false
    playerSection.Visible = false
    scriptsSection.Visible = true
    mainBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    espBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    playerBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    scriptsBtn.BackgroundColor3 = Color3.fromRGB(75, 0, 130)
end)

print("✅ Menu Venom chargé avec succès!")
print("Clé par défaut: " .. KEY)
print("🎮 Contrôle: T = Ouvrir/Fermer le menu")